using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelSetupHandler : MonoBehaviour
{
   [SerializeField] private List<GameModeMetaData> _gameModes = new();
   
   public void SetupLevel(GameMode mode)
   {
      
   }
}
